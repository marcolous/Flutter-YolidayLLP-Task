import 'package:flutter/material.dart';

class ProtfolioView extends StatelessWidget {
  const ProtfolioView({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView();
  }
}
